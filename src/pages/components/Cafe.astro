---
import CardOutlined from '../../components/CardOutlined.astro';
import Button from '../../components/Button.astro';
import styles from '../../styles/cafe.module.css';

const menu = {
  drinks: [
    {
      name: 'Jungle Brew',
      description: 'Bio-Kaffee aus nachhaltigem Regenwald-Anbau',
      price: '4.90',
    },
    {
      name: 'Golden Monkey Latte',
      description: 'Cremiger Latte mit Kokosblütensirup und Zimt',
      price: '6.50',
    },
    {
      name: 'Tropical Matcha',
      description: 'Matcha-Tee mit Passionsfrucht und Kokosmilch',
      price: '7.50',
    },
  ],
  food: [
    {
      name: 'Buddha Bowl',
      description: 'Quinoa, Avocado, Mango und geröstete Cashews',
      price: '18.50',
    },
    {
      name: 'Jungle Toast',
      description: 'Hausgemachtes Bananenbrot mit Beeren und Ahornsirup',
      price: '9.50',
    },
    {
      name: 'Paradise Acai Bowl',
      description: 'Frische Früchte, Granola und Kokosraspeln',
      price: '14.50',
    },
  ],
};
---

<section id="cafe" class={`layout-outer ${styles.root}`}>
  <div class="layout-inner">
    <div class="title-container">
      <h2 class={`section-title section-title--primary ${styles.title}`}>Jungle Café</h2>
    </div>
    <p class={styles.intro}>
      Tauchen Sie ein in unsere grüne Oase. Zwischen goldenen Tierskulpturen und üppigen Pflanzen
      servieren wir Ihnen ausgewählte Bio-Kaffeespezialitäten und exotische Köstlichkeiten. Ein
      Hauch von Dschungel-Atmosphäre mitten in Aarau.
    </p>

    <div class={styles.menuGrid}>
      <CardOutlined title="Getränke" variant="jungle">
        <div class={styles.menuItems}>
          {
            menu.drinks.map((item) => (
              <div class={styles.menuItem}>
                <div>
                  <h4 class={styles.menuItemTitle}>{item.name}</h4>
                  <p class={styles.description}>{item.description}</p>
                </div>
                <span class={styles.price}>CHF {item.price}</span>
              </div>
            ))
          }
        </div>
      </CardOutlined>

      <CardOutlined title="Speisen" variant="jungle">
        <div class={styles.menuItems}>
          {
            menu.food.map((item) => (
              <div class={styles.menuItem}>
                <div>
                  <h4 class={styles.menuItemTitle}>{item.name}</h4>
                  <p class={styles.description}>{item.description}</p>
                </div>
                <span class={styles.price}>CHF {item.price}</span>
              </div>
            ))
          }
        </div>
      </CardOutlined>
    </div>

    <div class={styles.cta}>
      <Button href="#contact" variant="primary">Reservierung anfragen</Button>
    </div>
  </div>
</section>
